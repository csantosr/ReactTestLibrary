[{"content":"import { screen, render, within } from '@testing-library/react';\n\nconst FormData = () => {\n  return (\n    <div>\n      <button>Go back</button>\n      <form aria-label=\"form\">\n        <button>Save</button>\n        <button>Cancel</button>\n      </form>\n    </div>\n  );\n};\n\nrender(<FormData />);","type":"code","id":"av3km"},{"content":"const toContainRole = (container, role, quantity = 1) => {\n  const elements = within(container).queryAllByRole(role);\n  if (elements.length === quantity) {\n    return {\n      pass: true\n    }\n  }\n  return {\n    pass: false,\n    message: () => `Expected to find ${quantity} ${role} elements. Found ${elements.length} instead`\n  }\n}\n\nexpect.extend({ toContainRole });","type":"code","id":"5iqve"},{"content":"test('the form displays two buttons', () => {\n  render(<FormData />);\n\n  const form = screen.getByRole('form');\n  expect(form).toContainRole('button', 2);\n});","type":"code","id":"wptsw"}]